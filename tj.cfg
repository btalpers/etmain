set fast "+sprint;set speed vstr slow;echo ^3Sprint on"
set slow "-sprint;set speed vstr fast;echo ^oSprint off"
set speed "vstr slow"
unbind shift
bind shift "vstr speed"

bind kp_leftarrow save
bind kp_end load

nofatigue
pmove_fixed 1